module.exports = {
    createTime: {
        type: Date,
        required: true,
        default: Date.now()
    },
    updateTime: {
        type: Date,
        required: true,
        default: Date.now()
    }
}